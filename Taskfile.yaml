# https://taskfile.dev

version: '3'

tasks:
  build:
    cmds:
      - go build -ldflags "-X github.com/noqqe/veilig/src/veilig.Version=`git describe --tags`" -v -o veilig main.go 
    sources:
      - "src/veilig/**/*.go"
      - "main.go"
    generates:
      - "./veilig"

  release:
    interactive: true 
    cmds:
      - git tag | tail -5
      - read -p "Version v1.1.1 " version ; git tag $version
      - git push --tags
      - goreleaser release --clean

  default:
    cmds:
      - task: build

